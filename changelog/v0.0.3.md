# v0.0.3 - 2026-03-15

## 变更主题
完成比赛版“用户分离”最小可用方案，新增数据归属能力，支持历史数据归档到测试账号，并实现按当前登录用户隔离条目与项目数据。

## 本次完成内容
- 为本地 `items` / `projects` 数据增加 `ownerUserId` 字段。
- 调整 Room DAO 查询逻辑，所有条目与项目读取改为按当前用户过滤。
- 调整 `ItemRepositoryImpl`，刷新数据时仅拉取当前用户对应的远端数据。
- 调整 `ProjectRepositoryImpl`，项目读取、创建、删除均绑定当前登录用户。
- 调整 `AuthRepository`，登录/注册后只保留当前会话用户，登出时同步清理当前用户本地缓存。
- 调整 `TokenManager`，明确提供当前用户 ID 获取方法，用于用户隔离逻辑。
- 兼容现网 NocoDB 真实归属字段 `ownerId`，并在代码中统一接入。
- 创建比赛测试归档账号 `gist_demo_archive`，用于承接历史测试数据。
- 已将历史数据迁移到测试归档账号：
  - `items` 40 条
  - `projects` 3 条
- 更新 README，补充比赛测试账号说明。
- 更新用户分离 PRD，补充比赛版迁移策略、测试账号规则与现网字段说明。

## 测试账号
- 用户名：`gist_demo_archive`
- 邮箱：`gist_demo_archive@example.com`
- 密码：`GistDemo@2026!`

## 影响文件
- `README.md`
- `USER_ISOLATION_PRD.md`
- `app/src/main/java/com/example/ai4research/core/security/TokenManager.kt`
- `app/src/main/java/com/example/ai4research/data/local/entity/ItemEntity.kt`
- `app/src/main/java/com/example/ai4research/data/local/dao/ItemDao.kt`
- `app/src/main/java/com/example/ai4research/data/local/dao/ProjectDao.kt`
- `app/src/main/java/com/example/ai4research/data/local/database/AI4ResearchDatabase.kt`
- `app/src/main/java/com/example/ai4research/data/mapper/ItemMapper.kt`
- `app/src/main/java/com/example/ai4research/data/remote/api/NocoApiService.kt`
- `app/src/main/java/com/example/ai4research/data/remote/dto/NocoItemDto.kt`
- `app/src/main/java/com/example/ai4research/data/repository/AuthRepository.kt`
- `app/src/main/java/com/example/ai4research/data/repository/ItemRepositoryImpl.kt`
- `app/src/main/java/com/example/ai4research/data/repository/ProjectRepositoryImpl.kt`

## 当前状态
- 测试账号登录后可读取原有历史数据。
- 新账号默认从空数据开始，不再直接看到历史公共测试数据。
- 新建项目与条目会绑定当前登录用户。
- 本地缓存已按比赛版方案做用户隔离与登出清理。

## 已知说明
- 当前现网 NocoDB 物理字段名为 `ownerId`，代码已按现网字段兼容。
- 由于当前 Gradle 环境存在依赖访问器生成阶段的文件锁问题，未拿到一次完整的本地编译通过记录；但已完成静态代码排查与实际账号、数据迁移验证。

## 版本备注
这是围绕“比赛版用户分离 MVP”完成的版本，目标是保证演示与答辩阶段的数据隔离能力成立，为后续正式认证体系升级打基础。
