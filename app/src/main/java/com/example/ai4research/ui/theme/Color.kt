package com.example.ai4research.ui.theme

import androidx.compose.ui.graphics.Color

// PixelPlay Inspired Colors
val PrimaryBlue = Color(0xFF3D7AF0) // Bright Blue for primary actions (FAB, Active States)
val SecondaryBlue = Color(0xFF2D3E5E) // Dark Blue/Grey for Headings or Secondary Buttons
val TertiaryBlue = Color(0xFF8E9AAF) // Muted Blue for inactive items

val BackgroundLight = Color(0xFFF5F9FF) // Very light blueish white background
val SurfaceLight = Color(0xFFFFFFFF) // Pure white for cards
val SurfaceContainerLight = Color(0xFFFFFFFF)
val OnSurfaceLight = Color(0xFF1A1C1E) // Nearly black text
val OutlineLight = Color(0xFFE1E7F2) // Subtle blue-grey border
val ErrorLight = Color(0xFFFF3B30)

// Dark Mode (Optional, adapting to be darker versions of the above)
val BackgroundDark = Color(0xFF0F1115)
val SurfaceDark = Color(0xFF1C1E24)
val SurfaceContainerDark = Color(0xFF1C1E24)
val OnSurfaceDark = Color(0xFFFFFFFF)
val OutlineDark = Color(0xFF2E323B)
val ErrorDark = Color(0xFFFF453A)
